<h1><%= @resource_class.name %></h1>

<%= render partial: '/shared/form', locals: { readonly: true } %>

<%= link_to 'Edit', edit_polymorphic_path(@resource), class: 'btn btn-primary' %>
<br/>
<%= link_to 'Back', polymorphic_path(@resource_class), class: "btn btn-info" %>