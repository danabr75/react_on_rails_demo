<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to Rails.configuration.app_name, admin_index_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if can?(:index, User) %>
        <li class="nav-item">
          <%= link_to 'Users', users_path, class: 'nav-link' %>
        </li>
        <% end %>

        <% if can?(:index, Blog) %>
        <li class="nav-item">
          <%= link_to 'Blogs', blogs_path, class: 'nav-link' %>
        </li>
        <% end %>

        <% if can?(:index, Member) %>
          <li class="nav-item">
            <%= link_to 'Team Members', members_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav" style="margin-left: auto;">
        <li class="nav-item">
          <%= link_to "Member View", root_path, class: 'btn btn-info' %>
        </li>
        <li class="nav-item">
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: 'btn btn-danger' %>
        </li>
      </ul>


    </div>
  </div>
</nav>
